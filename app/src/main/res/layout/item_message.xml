<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/white"
    android:foreground="?attr/selectableItemBackground"
    android:padding="16dp">

    <ImageView
        android:id="@+id/img_message_icon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/bg_circle_light_blue"
        android:padding="10dp"
        android:src="@drawable/ic_person"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@color/hazora_primary_blue"
        android:contentDescription="Sender Avatar" />

    <TextView
        android:id="@+id/tv_sender"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:text="User Name"
        android:textColor="@color/hazora_text_primary"
        android:textSize="17sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toStartOf="@id/tv_time"
        app:layout_constraintStart_toEndOf="@id/img_message_icon"
        app:layout_constraintTop_toTopOf="@id/img_message_icon"
        app:layout_constraintBottom_toTopOf="@id/tv_preview"
        app:layout_constraintVertical_chainStyle="packed" />

    <TextView
        android:id="@+id/tv_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="10:30 AM"
        android:textColor="@color/hazora_text_secondary"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/tv_sender" />

    <TextView
        android:id="@+id/tv_preview"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="Latest message content goes here..."
        android:textColor="@color/hazora_text_secondary"
        android:textSize="14sp"
        app:layout_constraintEnd_toStartOf="@id/view_unread_dot"
        app:layout_constraintStart_toStartOf="@id/tv_sender"
        app:layout_constraintTop_toBottomOf="@id/tv_sender"
        app:layout_constraintBottom_toBottomOf="@id/img_message_icon" />

    <View
        android:id="@+id/view_unread_dot"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:background="@drawable/bg_message_unread_dot"
        android:layout_marginStart="8dp"
        app:layout_constraintBottom_toBottomOf="@id/tv_preview"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/tv_preview" />

    <!-- Removed tv_subject and divider for cleaner chat look -->

</androidx.constraintlayout.widget.ConstraintLayout>
